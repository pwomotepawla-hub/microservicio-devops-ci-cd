version: '3.8'

services:
  microservicio:
    build: .
    container_name: microservicio-devops

    ports:
      - "5000:5000"

    restart: always

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000"]
      interval: 30s
      timeout: 10s
      retries: 3
